{% extends 'base.html.twig' %}

{% block title %}Activation du compte{% endblock %}

{% block body %}
<div class="activation-container">
  <div class="activation-card">
    <div class="activation-card-body">
      <h2 class="activation-title">🔐 Activation du compte</h2>
      <p class="activation-subtitle">Définissez votre mot de passe pour activer votre accès administrateur.</p>

      {# Flash messages #}
      <div class="flash-activate-container">
      {% for label, messages in app.flashes %}
        {% for message in messages %}
          <div class="flash-activate-message flash-{{ label }}">
            <span class="flash-activate-icon">⚠️</span> {{ message }}
            <button class="flash-activate-close" onclick="this.parentElement.style.display='none';">×</button>
          </div>
        {% endfor %}
      {% endfor %}
      </div>

      <form method="post" class="activation-form">
        <div class="form-group">
          <label for="newPassword">Mot de passe</label>
          <input type="password" name="newPassword" id="newPassword" placeholder="••••••••">
        </div>
        <div class="form-group">
          <label for="confirmPassword">Confirmer le mot de passe</label>
          <input type="password" name="confirmPassword" id="confirmPassword" placeholder="••••••••">
        </div>
        <button type="submit" class="activation-button">Activer mon compte</button>
      </form>
    </div>
  </div>
  <p class="activation-note">Ce formulaire est sécurisé. Votre mot de passe ne sera jamais partagé.</p>
</div>
{% endblock %}